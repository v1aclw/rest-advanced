{% extends 'base.html.twig' %}

{% block body %}
	<body class="d-flex flex-column h-100">
    <main class="flex-shrink-0">
            <!-- Navigation-->
            <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
                <div class="container px-5">
                    <a class="navbar-brand" href="{{ path('index') }}">REST advanced</a>
                    <div class="input-group ">
                        <input type="text" class="form-control" id="login" placeholder="Login" aria-label="Login" aria-describedby="signin"/>
                        <button class="btn btn-outline-light" type="button" id="signin">Sign in</button>
                        <button class="btn btn-outline-light" type="button" id="signout">Sign out</button>
                    </div>
                </div>
            </nav>
		{% block content %}{% endblock %}
        </main>
        <!-- Footer-->
        <footer class="bg-dark py-4 mt-auto">
            <div class="container px-5">
            
            </div>
        </footer>
	</body>
{% endblock %}
